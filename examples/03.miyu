pkg: Kaf;

import a.b.c, a.d, a.e;
import d.e.( a.d.e ), e.( d, p.g, f );


fn fib(d, g, em, a:int=7, b=4, c=4) : int ->
    let const a: int = 10, const b: int = 21;
    if x <= 0:
        ret p(0, d=3);
    elif x <= 2:
        ret 1;
    else:
        let a = 3;
    end;

    3 + a + (begin
    new A(3, 4)::<int, int>; end);
end;

fn main() ->
    for i in I: // iter(range(1, 20)):
        // print(("fib ", i, ": ", i));
        print(fib);
    end;
end;
